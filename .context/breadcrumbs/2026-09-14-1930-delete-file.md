# TASK-0022 — delete_file

**Status:** DONE (awaiting Evaluator)  
**Date:** 2026-09-14

## Requirement

Implement `delete_file(path)` as a quarantine move (never permanent delete): validate the input path, move one regular file into the configured quarantine folder with a timestamped name, support `dry_run=True` by default, and write audit records before and after the operation.

## Changes

- `agent/tools/delete_file.py` — quarantine move tool mirroring `move_file` structure (`os.link` + `os.unlink`, structured result dict).
- `tests/test_delete_file.py` — focused safety and execution tests aligned with `test_move_file.py`.
- `.context/current_state.md` — TASK-0022 marked implemented, next steps updated.
- `.context/sessions/S-0022-BLD-001.md` — Builder session note.

## Tests

- Command: `.venv\Scripts\python.exe -m pytest`
- Result: 64 passed in 1.17s

## Decisions

- Quarantine destination is not run through `validate_path()` because it may sit outside `WATCHED_FOLDERS`; only the user-supplied source path is validated, per task wording.
- Quarantine-path and already-in-quarantine refusals are detected before whitelist validation so messages match acceptance criteria.
- Default quarantine when `config.quarantine_folder` is unset: `REPO_ROOT / ".quarantine"`.
- Dry-run success writes audit `planned` then `ok` with `detail="dry_run"` (task edge case); validation refusals audit only when `dry_run=False`, matching `move_file`.

## Open questions

- None
